# Prometheus Setup

## Purpose
Prometheus collects metrics from
Jenkins and application endpoints.

## Port
- 9090

## Configuration
Targets are defined in prometheus.yml
for Jenkins and application services.
